# 작업 규칙

새 세션은 `docs/HANDOFF.md` 를 먼저 읽는다. 연구 방향, 파이프라인 구조, 확정된 사실, 보류한 작업이 거기 있다.

## 실험 · 분석

1. **사전등록은 실행 전에 단독 커밋한다.**
   - `docs/*_preregister.json` 을 쓰면 그 파일 하나만 먼저 커밋하고, 그 다음에 스크립트를 쓰고 돌린다. 사전등록에 무언가를 더할 때도 같다.
   - 탐색은 사전등록 없이 해도 된다. 대신 결과 파일에 «탐색용 · 논문 수치 아님» 을 적는다.
2. **참조 데이터 경로는 인자로 받는다.**
   - 채점 · 분석 스크립트는 사람 상자 · 가이드 · 손 찍기 같은 참조 데이터 경로를 argparse 인자로 받는다. 코드 안에 `~/Downloads/...` 같은 경로를 박지 않는다.
   - 지금 경로는 `eval/refs.json` 한 곳에만 둔다. 새 스크립트는 `refs.json` 에 경로를, `eval/run_all.py` 에 단계를 더한다.
   - 스크립트를 고치면 `python eval/run_all.py eval/refs.json` 으로 커밋된 결과가 바이트 단위로 같게 나오는지 확인한다.
3. **검증 자료가 필요해지면 `docs/VALIDATION_NEEDS.md` 에 항목을 더한다.** 항목마다 적는다: 무엇이 필요한가 · 어떤 분석에 쓰이나 · 몇 장·몇 개 · 누가 만들어야 하나(2인 독립 등) · 지금 임시로 쓰는 자료.
4. **사람이 만든 자료는 정답이 아니라 참조다.** 결과는 «정확도» 가 아니라 «참조와의 일치도» 로 적고, 라벨러 수를 함께 적는다. 현황은 `docs/HUMAN_DATA.md` 에 있다. 만든 이는 파일 안 필드나 문서의 말로만 적고, 커밋 작성자는 근거로 쓰지 않는다.
5. **결과를 보고 채택 범위 · 문턱 · 파라미터(detect / ground / rules)를 바꾸지 않는다.** 검증 실험에서는 부르기만 한다.
6. **옛 수치는 인용하기 전에 `docs/legacy_numbers.md` 를 본다.** 지금 코드로 재현되지 않는 수치를 찾으면 거기 적는다.
7. **VLM 상자.** 서버 안에서 VLM 을 부르지 않는다. 사람 상자를 본 적 없는 새 세션에서 원본 배율로 만들고, 모델명 · 날짜 · 지시문을 기록한다. 한 코퍼스 안에 상자 출처(사람 / VLM / 검출기)를 섞지 않고, 캐시에 provenance 를 남긴다.
8. **다른 디자이너를 넣는 조건**(로제 · 루더 · 호프만)은 음성으로 부르지 않는다. 사전등록에 달리 적지 않았으면 해석하지 않는다.
9. **합성 세트는 생성 뒤 사람이 이미지를 눈으로 확인한 다음에만 채점한다.**
   - 순서: 생성 → 자동 검증(겹침 · 판 밖 잉크 등) → 층별 표본 이미지 제시 (조작변인의 양 끝 수준이 든 판을 반드시 넣는다) → **사람 확인** → 검출 · 채점.
   - 사람 확인을 받기 전에는 Surya 검출도 VLM 도 돌리지 않는다.

## 문서

- **폐기된 세트, 실패한 설계, 버그 발견 경위는 저장소 문서(`docs/` 의 해부 · 인수인계 문서 등)에만 남긴다.** `docs/RESULTS_FOR_PAPER.md` 는 논문에 들어갈 것만 담는다 — 최종 설계와 그 결과만 넣고, 폐기 사실 자체도 적지 않는다. 폐기 표시는 그 세트의 해부 문서(묶기 비교 240장은 `docs/GROUP_SET_ANATOMY.md`)에만 둔다.
- 폐기는 표시로 한다. 파일과 커밋은 지우지 않는다. 결과 파일에 «폐기» 필드를, `eval/refs.json` 에 폐기 표시를 두어 `run_all` 이 다시 채점하지 않게 한다.

## 커밋 · 보고

- 커밋은 사용자가 지시할 때만 한다. 푸시는 따로 묻는다.
- 보고는 표 하나와 세 줄 요약으로 한다. 예상과 다른 결과를 먼저 적는다. «단계마다 보고하고 멈춰» 라는 지시가 있으면 그 단계에서 멈춘다.

## 환경

- Python 은 `.venv/bin/python`. 셸은 zsh 다 (따옴표 없는 변수가 단어로 쪼개지지 않는다).
- MCP 서버 `typo` 는 이 폴더의 `server.py` 를 작업 트리 그대로 돌린다. `tools/` 가 import 하는 파일을 옮기면 MCP 가 깨진다.
- 캐시는 `~/.typo-mcp/` 에 있다 (저장소 밖).
